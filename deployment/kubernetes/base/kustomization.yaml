apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - workload.yaml
  - service.yaml
  - network-policy.yaml
images:
  - name: reference-app
    # Replace both fields with your own: the registry your cluster can pull
    # from, and the digest your Chapter 4 build produced and Chapter 6
    # promoted. The digest below is an all-zero placeholder, and an all-zero
    # digest cannot pull - deliberately, so that an unreplaced value fails at
    # admission instead of deploying something you never reviewed.
    # See ../README.md, and ../../gitops/lab-source/README.md for the three
    # things you supply before Chapter 9 can reconcile.
    newName: localhost:5001/reference-app
    digest: sha256:e17a63fde89d1757f3dafb721f92c2d9fba5979ea90a86e137af2a4ba2127d9e
