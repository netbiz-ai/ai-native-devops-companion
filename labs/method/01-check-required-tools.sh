#!/usr/bin/env bash
# Chapter 1, Prerequisites / Required tools - confirm bash and git are installed
#
# Label: Runnable
#
# Expected result, per the chapter:
#   Each command should print an installed path.
# --- command as printed, verbatim ---
command -v bash
command -v git
