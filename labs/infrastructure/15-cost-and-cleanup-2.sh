#!/usr/bin/env bash
# The infrastructure lab - CH05, Cost and Cleanup
#
# Label: Runnable - destructive to local initialization data only
# --- command as printed, verbatim ---
rm -rf infrastructure/terraform/environments/dev/.terraform
